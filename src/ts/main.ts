window.setup = () => {
    // Example Defaults
    // createCanvas(100, 100);
    // createCanvas(100, 100, WEBGL);

    // Custom setup
};

window.draw = () => {
    // Example Defaults
    // background(200);
    // fill(255);
    // stroke(0);

    // Custom draw
};